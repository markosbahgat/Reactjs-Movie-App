/*

import { createSlice } from "@reduxjs/toolkit";

const movieSlice = createSlice({
    name:"movie",
    initialState : {},
    reducers:{
        postMovie: (state, action) => { console.log(state, action) },
        updateMovie: (state, action) => { console.log(state, action) },
        deleteMovie: (state, action) => { console.log(state, action) },
    },
    extraReducers:{
        //here we will add some reducer that will listen to another reducers or to listen to thunk middelware while fetching from api
    }
})

*/